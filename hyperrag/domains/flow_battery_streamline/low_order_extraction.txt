{DOMAIN_CONTEXT}

Extract direct pairwise relations between the entities below.

CRITICAL:
- source and target must be exact names from K_v.
- Do not invent missing entities.
- Do not put numeric values in source/target names.
- If a relation needs a missing concept, omit the relation.
- Use the closest exact K_v entity when it is clearly equivalent in the text.
  Example: use "VO2+/VO^2+" if K_v has that redox couple and the text sentence refers to the same vanadium ions.

RELATION TYPES:
- COMPOSITION: component, precursor, product, additive, dopant, solvent, membrane/electrode modification.
- OPERATION: co-present in the same experiment, condition, device, or performance measurement.
- DEGRADATION: causes, mitigates, indicates, explains, or suffers a failure phenomenon.
- COMPARISON: compared under a shared condition, metric, or design variable.

Entities (K_v):
{K_v_JSON}

Text:
{CHUNK_TEXT}

OUTPUT FORMAT - strict JSON array only:
[
  {{
    "source": "<entity name from K_v>",
    "target": "<entity name from K_v>",
    "relation_type": "COMPOSITION | OPERATION | DEGRADATION | COMPARISON",
    "description": "<why related>",
    "keywords": "<overarching concepts>",
    "strength": <0-10 integer>
  }}
]

Output ONLY the JSON array. No prose before or after.
