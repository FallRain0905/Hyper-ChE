{DOMAIN_CONTEXT}

Extract high-order hyperedges connecting 3 or more entities from K_v.

CRITICAL:
- vertices must contain exact entity names from K_v only.
- Do not invent missing entities.
- Do not put numeric values in vertex names.
- Prefer hyperedges that connect material/species + condition + metric or mechanism.
- Use broader exact K_v entities instead of inventing narrower vertices.
  Example: use "vanadium redox flow battery" if K_v contains it and the sentence uses "VRFB".

HYPEREDGE TYPES:
- COMPOSITION: formulation or material design involving 3+ components/variables.
- OPERATION: experiment linking species/material/system + condition + performance metric.
- DEGRADATION: mechanism linking species/material + failure phenomenon + evidence/metric/condition.
- COMPARISON: multiple candidates compared under a shared metric or condition.

Entities (K_v):
{K_v_JSON}

Text:
{CHUNK_TEXT}

OUTPUT FORMAT - strict JSON array only:
[
  {{
    "vertices": ["<entity name from K_v>", "<entity name from K_v>", "..."],
    "relation_type": "COMPOSITION | OPERATION | DEGRADATION | COMPARISON",
    "description": "<detailed description covering all entities>",
    "generalization": "<one-sentence summary>",
    "keywords": "<overarching concepts>",
    "strength": <0-10 integer>,
    "evidence_span": "<verbatim quote from text, <=120 chars>"
  }}
]

Output ONLY the JSON array. No prose before or after.
